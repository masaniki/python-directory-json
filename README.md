# Abstract
未完成。

# Test Method.
Run the following command.
```
python src/directory_json.py . -f tests/output.json
python tests/test_dirjson.py
```
If `AssertionError` is not found, the test is success.
